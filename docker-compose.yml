services:
  postgres:
    image: postgres:18.1
    container_name: postgresql
    ports:
      - "5432:5432"
    command: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=data-encryption-demo
